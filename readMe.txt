There are two methods of running this project

Either open it in your preferred development software (Pycharm, etc.) and run it through the controller.py

or

Navigate to the directory through your terminal and run the command "python controller.py"